
compiler=g++
archiver=ar
output.file.name=libfoxmake.a

src.dir=src
build.dir=build

IF os == "windows"
    bin.dir=bin/Release/windows
ENDIF
IF os != "windows"
    bin.dir=bin/Release/linux
ENDIF

obj.dir=obj/Release/archive
compiler.params=-Wall -fexceptions -O2 -std=c++17
archiver.params=